<!-- Content Header (Page header) -->
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1><%= title  %> </h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="/admin">Home</a></li>
                    <li class="breadcrumb-item active">Testimonials</li>
                </ol>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>

<!-- Main content -->
<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">
                        <h3 class="card-title">All Testimonials</h3>
                        <a href="/admin/testimonial/create" class="btn btn-success btn-lg float-right rounded">Create</a>
                    </div> 
                    <!-- /.card-header -->
                    <div class="card-body">
                        <% if (testimonials && testimonials != [] ) { %>
                            <table class="table table-bordered table-responsive">
                                <thead>
                                    <tr>
                                        <th style="width: 10px">#</th>
                                        <th>Name</th>
                                        <th>Designation</th>
                                        <th>Title</th>
                                        <th>Description</th>
                                        <th>Created On</th>
                                        <th>Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% testimonials.forEach((testimonial,index) => { %>
                                        <tr>
                                            <td><%= index + 1 %> </td>
                                            <td><%= testimonial.name  %> </td>
                                            <td><%= testimonial.designation  %> </td>
                                            <td><%= testimonial.title  %> </td>
                                            <td><textarea readonly><%= testimonial.description  %> </textarea></td>
                                            <td>
                                               <%= new Date(testimonial.createdAt)  %> 
                                            </td>
                                            <td>
                                                <a href="/admin/testimonial/view/<%= testimonial._id %> "><i class="fas fa-eye"></i></a>
                                                <a href="/admin/testimonial/update/<%= testimonial._id %> "><i class="fas fa-pen"></i></a>
                                                <a href="/admin/testimonial/delete/<%= testimonial._id %> "><i class="fas fa-trash"></i></a>
                                            </td> 
                                        </tr>
                                    <% }) %>
                                </tbody>
                            </table>
                        <% } else { %>
                            <h1>No Records Found</h1>
                        <% } %> 
                         
                    </div>
                    <!-- /.card-body -->
                </div>
                <!-- /.card -->
            </div>
            <!-- /.col -->
        </div>
    </div><!-- /.container-fluid -->
</section>
<!-- /.content -->